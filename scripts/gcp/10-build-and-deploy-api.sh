#!/usr/bin/env bash
# F5 stub — populated when we deploy to GCP.
# Will: gcloud builds submit + gcloud run deploy (Cloud SQL, secrets, SA).
set -euo pipefail
echo "TODO(F5): build & deploy api to Cloud Run. See README §F5."
