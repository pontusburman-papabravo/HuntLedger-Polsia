#!/usr/bin/env bash
# F5 stub — populated when we deploy to GCP.
# Will: start Cloud SQL Auth Proxy and run migrations.
set -euo pipefail
echo "TODO(F5): run migrations against Cloud SQL via auth proxy. See README §F5."
