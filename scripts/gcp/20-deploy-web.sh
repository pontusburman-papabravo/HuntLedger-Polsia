#!/usr/bin/env bash
# F5 stub — populated when we deploy to GCP.
# Will: build apps/web and `firebase deploy --only hosting`.
set -euo pipefail
echo "TODO(F5): build web and deploy to Firebase Hosting. See README §F5."
