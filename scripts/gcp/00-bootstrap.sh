#!/usr/bin/env bash
# F5 stub — populated when we deploy to GCP.
# Will: enable APIs, create Artifact Registry, Cloud SQL, runtime SA, secrets.
set -euo pipefail
echo "TODO(F5): bootstrap GCP project. See README §F5."
